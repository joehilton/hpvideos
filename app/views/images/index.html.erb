<div class="maincontainer" id="masonry-container" class="clearfix">
	<% @images.each do |i| %>
		<div class="box col3"><%= link_to image_tag(i.image.url(:medium)), image_path(i.id), :method => :delete %></div>
	<% end %>
</div>
